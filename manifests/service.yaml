---
apiVersion: v1
kind: Service
metadata:
  name: sample-app-svc
  namespace: devops-assignment
  labels:
    app: sample-app
spec:
  type: ClusterIP
  selector:
    app: sample-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP